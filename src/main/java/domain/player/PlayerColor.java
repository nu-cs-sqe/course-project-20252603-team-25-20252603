package domain.player;

/**
 * The four player tokens available in a standard CATAN game. Order is
 * presentation-only; turn order is decided by {@link domain.turn.TurnOrder}.
 */
public enum PlayerColor {
    RED,
    BLUE,
    WHITE,
    ORANGE
}
