package domain.deck;

/**
 * The five kinds of development card available in standard CATAN. The deck
 * composition (how many of each) is owned by {@link DevelopmentCardDeck}.
 */
public enum DevelopmentCardType {
    KNIGHT,
    VICTORY_POINT,
    ROAD_BUILDING,
    MONOPOLY,
    YEAR_OF_PLENTY
}
